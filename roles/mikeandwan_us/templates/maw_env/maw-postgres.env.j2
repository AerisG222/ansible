# set the postgres password when initializing the container w/ blank db
POSTGRES_PASSWORD={{ maw__sql_postgres_password }}

# set the pwd for use in client tooling
PGPASSWORD={{ maw__sql_postgres_password }}

# set the catchall host method in pg_hba.conf (used when first creating container/config)
POSTGRES_HOST_AUTH_METHOD=scram-sha-256

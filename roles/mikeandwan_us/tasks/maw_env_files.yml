---
- name: Make sure maw_env directory exists
  ansible.builtin.file:
    path: "{{ maw__podman_env_dir }}"
    state: directory
    mode: u+rwx

- name: Prepare environment files
  ansible.builtin.template:
    backup: "{{ maw__env == 'prod' }}"
    src: "maw_env/{{ item }}.j2"
    dest: "{{ maw__podman_env_dir }}/{{ item }}"
    mode: u+rw
  loop:
    - maw-postgres.env
    - maw-reverse-geocode.env

- name: Add env files to .bash_profile for dev
  ansible.builtin.blockinfile:
    path: "{{ maw__bash_profile }}"
    block: |
      function load_dotenv(){
        # https://stackoverflow.com/a/66118031/134904
        source <(cat $1 | sed -e '/^#/d;/^\s*$/d' -e "s/'/'\\\''/g" -e "s/=\(.*\)/='\1'/g")
      }

      set -o allexport

      [ -f "{{ maw__podman_env_dir }}/maw-api.env" ] && load_dotenv "{{ maw__podman_env_dir }}/maw-api.env"
      [ -f "{{ maw__podman_env_dir }}/maw-auth.env" ] && load_dotenv "{{ maw__podman_env_dir }}/maw-auth.env"
      [ -f "{{ maw__podman_env_dir }}/maw-www.env" ] && load_dotenv "{{ maw__podman_env_dir }}/maw-www.env"
      [ -f "{{ maw__podman_env_dir }}/maw-cache-sync.env" ] && load_dotenv "{{ maw__podman_env_dir }}/maw-cache-sync.env"

      set +o allexport
  when: maw__env == 'dev'

---
- name: ensure we have latest images for mikeandwan.us
  containers.podman.podman_image:
    name: "{{ item }}"
  loop:
    - "{{ mawus__podman_image_postgres }}"
    - "{{ mawus__podman_image_solr }}"
    - "{{ mawus__podman_image_photos }}"
    - "{{ mawus__podman_image_files }}"
    - "{{ mawus__podman_image_auth }}"
    - "{{ mawus__podman_image_api }}"
    - "{{ mawus__podman_image_www }}"
    - "{{ mawus__podman_image_gateway }}"
    - "{{ mawus__podman_image_maw_certs }}"
    - "{{ mawus__podman_image_certbot }}"
    - "{{ mawus__podman_image_solr_indexer }}"
    - "{{ mawus__podman_image_reverse_geocode }}"
    - "{{ mawus__podman_image_gdrive }}"
  when: item | length > 0

---
- name: ensure asset directories exist
  become: true
  ansible.builtin.file:
    path: "{{ item }}"
    # recurse: true
    state: directory
    owner: root
    mode: u+rwx,go+rx
  loop: "{{ asset_dirs }}"

# todo: copy files for prod

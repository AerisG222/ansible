---
all:
  children:

    servers:
      hosts:
        tifa:
          hostname: tifa
          ansible_user: mmorano

    workstations:
      hosts:
        barett:
          ansible_connection: local
          has_nvidia_gpu: yes
          hostname: barett

  vars:
    domain: mikeandwan.us

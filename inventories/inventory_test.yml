---
all:
  children:

    workstations:
      hosts:
        lvmtest:
        vmwstest:

    ddclient:
      hosts:
        lvmtest:
        vmtest:

    mawus:
      hosts:
        lvmtest:
        vmtest:
